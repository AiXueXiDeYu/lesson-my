const config = {
    port: 3000,
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: ' ',
        HOST: 'localhost',
        PORT: 3000
    }
}
module.exports = config